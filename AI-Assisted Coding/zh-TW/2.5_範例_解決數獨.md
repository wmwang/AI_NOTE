# 2.5 範例：解決數獨

在最後這個範例中，我們將展示如何結合 ChatGPT 或其他 AI 工具，開發一個解決數獨遊戲的 Java 程式。最初的情況是：您有自己的想法，但希望借助 AI 的協助來避免繁瑣的打字工作。因此，您給出明確的指令，說明 AI 應該生成哪些程式碼，然後將其整合到您的程式中，最後自行進一步開發，直到遇到問題為止。如果您願意的話，您就是「資深開發者」，可以將不愉快的工作委派給「初級 AI 開發者」。

## 2.5.1 類別設定

按部就班地工作，首先您指定程式的結構。您讓 AI 機器人幾乎沒有選擇的餘地。提示中包含了您的知識背景、偏好的程式語言、計劃的程式結構，以及用於儲存數獨遊戲的資料結構等資訊。

> **單一提示的數獨求解器**
> 在本節結束時，我們會展示您也可以用單一提示獲得一個數獨求解器，但這不是您的目標。俗話說：「重要的是過程，而不是結果」，而在這個過程中，您希望深化您的程式碼結構技能。

您明確要求 AI 聊天系統建立一個包含實例變數和建構子的 Sudoku 類別，但不要有其他方法。此外，AI 工具還應實作 Main 類別並在那裡初始化一個簡單的數獨謎題。

**提示：** I am an intermediate Java programmer. I want to write a program to solve sudokus step-by-step with your assistance. Please do NOT provide the complete code. The sudoku is to be stored in a 9 × 9 int array. 0 means this cell is empty. Please provide the Sudoku class with a private instance variable and a constructor. Also provide the Main class with a simple but incomplete sudoku.

```java
import ...;
public class Sudoku {
    private int[][] grid;
    public Sudoku(int[][] grid) {
        if (grid.length != 9 || grid[0].length != 9) {
            throw new IllegalArgumentException("Grid must be a 9x9 array.");
        }
        for (int i = 0; i < 9; i++) {
            for (int j = 0; j < 9; j++) {
                if (grid[i][j] < 0 || grid[i][j] > 9) {
                    throw new IllegalArgumentException("Grid values must be between 0 and 9.");
                }
            }
        }
        this.grid = new int[9][9];
        for (int i = 0; i < 9; i++) {
            for (int j = 0; j < 9; j++) {
                this.grid[i][j] = grid[i][j];
            }
        }
    }
}
```

## 2.5.2 分離行中的單一空格

現在您可以按照解決數獨的方式繼續：遍歷所有行，如果只有一個空格，填入唯一缺少的數字。

**提示：** Perfect. Now we start the solving code. As a first step, I want to loop through the grid to find rows with exactly one empty cell. I am unsure how to name this method. Do you have any ideas?

ChatGPT 提供了四個方法名稱供選擇，並建議使用 `fillSingleEmptyInRow`。

**提示：** Please replace i and j with row and column. Add a counter to return the number of cells filled.

```java
public int fillSingleEmptyInRow() {
    int cellsFilled = 0;
    for (int row = 0; row < 9; row++) {
        int emptyCount = 0;
        int emptyColumn = -1;
        for (int column = 0; column < 9; column++) {
            if (grid[row][column] == 0) {
                emptyCount++;
                emptyColumn = column;
            }
        }
        if (emptyCount == 1) {
            boolean[] present = new boolean[10];
            for (int column = 0; column < 9; column++) {
                present[grid[row][column]] = true;
            }
            for (int number = 1; number <= 9; number++) {
                if (!present[number]) {
                    grid[row][emptyColumn] = number;
                    cellsFilled++;
                    break;
                }
            }
        }
    }
    return cellsFilled;
}
```

## 2.5.3 解決列和區塊中的單一空格

您要求 ChatGPT 為列和 3 × 3 區塊撰寫兩個等效的方法。

**提示：** Please provide two more methods similar to fillSingleEmptyInRow to handle columns and 3 × 3 areas with only one cell missing.

## 2.5.4 測試程式碼和測試資料的問題

**提示：** Please provide code for two more methods: one to count the empty cells, and another to print the sudoku with empty spaces instead of 0.

ChatGPT 生成的範例數獨太難了，經過三次嘗試仍未能提供足夠簡單的謎題。

## 2.5.5 腦力激盪

**提示：** So far, my program can solve only very simple sudokus. Can you suggest a more sophisticated solution method? Just present the idea, no code please.

ChatGPT 提出了裸對/裸三、隱對/隱三、X-wing 等概念和遞迴回溯演算法。

## 2.5.6 儲存每個欄位的解決候選

**提示：** I want to add an instance variable with 9 × 9 sets to store candidates for each cell. Is `private Set<Integer>[][] candidates;` the correct data type?

**提示：** Next, I need a method to loop over all the cells. For each cell, remove entries from the candidate set if the same number is used within the row, column, or 3 × 3 block.

## 2.5.7 填入只有一個解決數字的欄位

**提示：** Write a method to loop over all the cells that are still empty. If the candidate set contains exactly one number, set that cell accordingly.

程式現在可以解決中等難度的謎題。Sudoku 類別現在有將近 300 行。

## 2.5.8 個人結論

幾年前使用 Kotlin 不靠 AI 花了兩天，現在靠 AI 兩小時就達到相同進度！AI 生成的程式碼清楚可理解。效果好的原因：網路上有充足的訓練材料，且我自己有足夠的知識來引導開發方向。

這正是 AI 在程式設計中使用的理想條件：您知道自己在做什麼，不會被 AI 生成程式碼的複雜性所壓倒。

## 2.5.9 替代程序

**提示：** Can you show me a short, concise way to solve a sudoku with Java?

大多數 AI 工具建議回溯演算法——將數字 1 到 9 依次插入，如果出錯則回溯。優點：程式碼只需 70 行，可應對任何難度。缺點：完全「非人類」的方法。

## 2.5.10 正確的提示方式

我們認為讓 AI 完全代勞是錯誤的：
- 如果您仍在學習，逐步方式將為您提供基本理解
- 您應該始終在您的知識範圍內使用 AI 工具，而不是超越它