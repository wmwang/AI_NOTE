# 8.2 範例：將 Python 腳本轉換為 PHP 程式碼

以下範例同樣來自我們的日常工作，但範圍較廣。起點是一個約 100 行的 Python 腳本。它讀取一個包含資料庫名稱的文字檔。然後，腳本分析超過 100 個相似的資料庫，並建立客戶活動的統計摘要。結果是一個難以閱讀的 ASCII 表格，每月透過電子郵件發送。

我們想將它轉換為一個可以隨時呼叫的 PHP 頁面。結果應以 HTML 表格的形式呈現。對於程式碼轉換，我們使用了 ChatGPT 4o；所有其他工作都在 VS Code 中使用 GitHub Copilot 完成。

在這個範例中，我們將重點放在解釋提示。我們省略了印出非常專案特定的 PHP 或 Python 程式碼，因為它對理解這個流程並沒有真正的幫助。

**提示：** 我有一個每月執行一次的 Python 腳本，用來彙總帳戶活動。我想將這個腳本轉換為 PHP。結果要以 HTML 表格而非 ASCII 呈現。能幫我嗎？

[100 行 Python 程式碼]

當然！以下是將提供的 Python 腳本轉換為 PHP 並修改為以 HTML 表格呈現結果的方法。

[170 行 PHP 程式碼]

[更多細節、說明、執行腳本]

產生的程式碼乍看之下留下了不錯的印象。但我們有兩個根本性的問題：

首先，ChatGPT 在輸出結果時將 HTML 程式碼與嵌入的 PHP 程式碼混在一起。這在 PHP 中相當常見，但我們偏好一致的 PHP 程式碼，所有輸出都透過 `echo` 執行。這樣我們比較容易掌握程式碼的全貌。

其次，程式碼沒有以函式的形式組織。然而，我們希望將統計資料的輸出與其他子功能分開。

因此，我們要求改進。為此，我們首先選擇一個程式碼區段。以下提示參照所選的程式碼。`result` 是一個在前述程式碼中定義的變數，包含一個陣列，其中儲存了從資料庫中先前確定的資料。高亮的程式碼分析這些資料並將其轉換為 HTML 表格。

**提示：** 你能將這段程式碼轉換為一個函式（帶有參數 `$result`）並使用 `echo` 輸出嗎？

當然！以下是如何將提供的 Python 腳本轉換為 PHP 並修改為以 HTML 表格呈現結果的方法。

[完整程式碼，包括新函式 `displayAccountActivitySummary`...]

在此基礎上，我們將 ChatGPT 產生的程式碼複製到 VS Code 並在那裡進行各種調整。特別是，我們使用了一個用於建立 MySQL 資料庫連線的類別，這個類別已在我們的 PHP 專案中可供使用。在 VS Code 中，GitHub Copilot 支援了我們的工作。

開始工作僅十五分鐘後，我們就能進行初次測試，整體結果令人滿意。在接下來的十五分鐘裡，我們優化了 HTML 表格的佈局，改善了欄位的標籤，並為表格添加了圖例。GitHub Copilot 以幾乎有預知能力的文字建議讓我們驚訝，我們幾乎原封不動地採用了這些建議，只做了極少的修改。

ChatGPT 原封不動地採用了 Python 腳本中的結果排序標準。這基本上可行，但我們想要有第二個排序標準。然而，我們對 ChatGPT 使用的 PHP 函式 `usort` 沒有太多經驗。

**提示：** PHP/usort：如何使用兩個標準而不是一個來排序？

我們原本預期一個通用的答案。然而，ChatGPT 回覆了幾行程式碼，其中正好使用了我們本來就想作為第二排序標準的結果屬性。不需要任何進一步的修改，程式碼就做到了我們想让它做但沒有說出來的事情。這有點令人毛骨悚然。

## 8.2.1 結論

這裡展示的新 PHP 腳本開發路徑是 AI 工具運作良好的一個典型範例。我們自己也很驚訝能如此快速地達成目標。在您質疑這是一個人為構造的範例之前，請注意，我們這裡概述的工作是為一個與本書無關的專案進行的。我們只是在後來才想到在這一章中展示這些提示。所以，這個範例確實是源自於我們的真實工作。
